console.log("🔧 Manually restoring component positions..."); const components = JSON.parse(localStorage.getItem("prototypeComponents") || "[]"); const restoredComponents = components.map(c => { if (c.position.x === 100 && c.position.y === 100) { console.log(`Restoring component ${c.id} to frame center`); const frames = JSON.parse(localStorage.getItem("prototypeFrames") || "[]"); const frame = frames.find(f => f.id === c.frameId); if (frame) { return { ...c, position: { x: frame.position.x + 50, y: frame.position.y + 50 } }; } } return c; }); localStorage.setItem("prototypeComponents", JSON.stringify(restoredComponents)); console.log("✅ Components restored! Refresh the page.");
